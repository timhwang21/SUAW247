import React, { Component } from 'react';

import { Panel } from '../../../../../../components/layout';

import './Now.css';

class Now extends Component {
  render() {
    return (
      <Panel id="Now">
        Now
      </Panel>
    );
  }
}

export default Now;